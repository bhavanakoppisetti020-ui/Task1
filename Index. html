<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Retirement Age Calculator</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
      max-width: 400px;
      margin: auto;
    }
    label, input, button {
      display: block;
      margin: 10px 0;
    }
    #result {
      margin-top: 20px;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <h1>Retirement Age Calculator</h1>
  
  <label for="birthYear">Enter your birth year:</label>
  <input type="number" id="birthYear" placeholder="e.g., 1980" />
  
  <button onclick="checkRetirement()">Check Retirement Eligibility</button>
  
  <div id="result"></div>
  
  <script>
    function checkRetirement() {
      const retirementAge = 65;
      const currentYear = new Date().getFullYear();
      const birthYearInput = document.getElementById('birthYear').value;
      const birthYear = parseInt(birthYearInput);
      
      if (!birthYear || birthYear > currentYear || birthYear < 1900) {
        document.getElementById('result').textContent = 'Please enter a valid birth year.';
        return;
      }
      
      const age = currentYear - birthYear;
      const yearsLeft = retirementAge - age;
      
      if (age >= retirementAge) {
        document.getElementById('result').textContent = `You are ${age} years old and eligible for retirement. 🎉`;
      } else {
        document.getElementById('result').textContent = `You are ${age} years o
